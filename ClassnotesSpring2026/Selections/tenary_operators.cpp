#include <iostream>

using namespace std;

int main()
{
    string even_odd;

    int age = 18;

    even_odd = (age%2 == 0) ? "Even": "Odd";

    if(age%2 == 0)
        even_odd = "Even";
    else
        even_odd = "Odd";


    if(month == 0)
        result = "jan";
    else if(month == 1)
        result = "feb";
        


    return 0;
}
