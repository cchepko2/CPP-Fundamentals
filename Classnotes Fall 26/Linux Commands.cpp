// This is a comment
/*
Author: Corin Chepko
Date: 8/19/26
Program Description: Just linux commands, this is not a c program
*/

// First command: ls (stands for list), it lists the contents of a folder
// ls -a (lists all folders and files inclucing hidden ones)

// cd (stands for change directory)
// cd <folder name> (moves within the folder name)
// cd .. (moves up into the parent directory)

// mkdir    (creates a directory)
// rm <filename>
// rm -r    (rm a directory and all things inside)
// rm -r -f (rm a directory and everything in it and don't ask me questions)
// cat      (list contents of a text file)
// cp <filename> <location>
// mv <filename or directory> <new filename or directory>

// Git Commands
    // git add -A    (stages all changes)
    // git commit -m "A message" (commits changes)
    // git push     (push changes to GitHub)
    // git pull     (pulls changes from GitHub if changes were made on another system)
